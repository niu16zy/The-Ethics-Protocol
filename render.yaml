services:
  - type: web
    name: ethics-protocol
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: LOGIC_FORTRESS_LLM_PROVIDER
        value: fox
      - key: FOX_API_KEY
        sync: false
